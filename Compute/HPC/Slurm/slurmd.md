One per compute node. Launches and manages tasks. It is small and light

Supports [[Hierarchical Communications]] with configurable fanout


To get information about a compute node, you can run `slurmd -C`